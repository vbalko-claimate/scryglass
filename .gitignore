# Runtime databases
*.db
*.db-shm
*.db-wal
data/backups/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/

# Environment
.env

# OS
.DS_Store

# IDE
.idea/
.vscode/

# Claude Code
.claude/
mtga_collection_raw.json
src-tauri/target/
build/
dist/
data/log_archive/
data/meta/*.bak
mtga_collection_raw_pre_*.json
mtga_inventory.json

# Build artifacts — regenerated by CI (release.yml) / tools/build_and_test.sh.
# Sidecar binaries (glass-host/glass-server/overlay-helper) + the compiled
# advise card DB are never committed; they are produced from glass-shard.
src-tauri/binaries/
src-tauri/resources/glass_advise_db.json

# Local replay-capture experiments. The curated, tracked corpus is
# data/replay_corpus/*.json; these dated/variant dirs are working data.
data/replay_corpus_*/
